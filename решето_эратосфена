#Решето Эратосфена
N = int(input())
A = [True] * N
A[0] = A[1] = False
for k in range(2, N):
    if A[k]:
        for i in range(k + k, N, k):
            A[i] = False
print(A)
for i in range(N):
    print(i, '-', 'простое' if A[i] else 'составное')
